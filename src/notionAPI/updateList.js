import { getToken } from './authentication';

const token = getToken();

export async function updateList(newInput) {
  // do work to make api call with token and newInput

  console.log('returned message');
}
